console.log('App.js is running now!');

// JSX - JavaScript XML
var template = <h1 id="heading">Indecision App!</h1>;
var appRoot = document.getElementById('app');

ReactDOM.render(template, appRoot);
